// Largest of three numbers
module largest_of_three_numbers;
  int a = 10, b = 25, c = 15;

  initial begin
    if(a > b && a > c)
      $display("Largest = %0d", a);
    else if(b > c)
      $display("Largest = %0d", b);
    else
      $display("Largest = %0d", c);
  end
endmodule
